import React from 'react';

import TagSection from '../TagSection';

import './styles.css';

export default function Footer({}) {
    return (
        <footer>
            <TagSection />
        </footer>
    )
}